# Optional dependencies for the CLAP smart-sequencing layer.
# Install only when enabling config.CLAP.enabled=True:
#
#   pip install --user --break-system-packages -r requirements-clap.txt
#
# Heavy: torch alone is ~800 MB on disk. First CLAP inference downloads
# the laion/clap-htsat-fused checkpoint (~1.7 GB) into ~/.cache/huggingface.

# CLAP via HuggingFace transformers
transformers>=5.0.0,<6.0.0
torch>=2.0.0
torchaudio>=2.0.0

# Vector similarity index for smart_queue.SmartQueueIndex
faiss-cpu>=1.7.0,<2.0.0

# librosa is already in requirements.txt; listed here for completeness as
# it's the audio loader used by audio_embeddings.compute_embedding().
librosa>=0.10.0,<1.0.0
