# Radio Pipeline Dependencies
# Python 3.10+ required

# HTTP client with retry support
requests>=2.31.0,<3.0.0
urllib3>=2.0.0,<3.0.0

# Audio processing
essentia-tensorflow>=2.1b6.dev1110
librosa>=0.10.0,<1.0.0
# numpy 2.x is what production runs (2.3.5 validated with essentia/librosa/torch)
numpy>=1.24.0,<3.0.0

# Audio metadata
mutagen>=1.47.0,<2.0.0

# Fuzzy string matching for YouTube track matching
rapidfuzz>=3.0.0,<4.0.0

# Configuration (.env loading is done by pydantic-settings, which pulls python-dotenv)

# RSS / Atom parsing (multi-source discovery)
feedparser>=6.0.0,<7.0.0

# Development / test (optional, not needed for production runs)
# pytest>=8.0.0

# Validation
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
